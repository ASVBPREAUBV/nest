export const host_protocol = 'http';
export const host = 'localhost';
export const host_port = 3000;
export const host_websocket_port = 3001;
export const host_url = host_protocol + '://' + host + ':' + host_port;
export const host_websocket_url = host_protocol + '://' + host + ':' + host_websocket_port;